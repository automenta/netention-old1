NodePropertyPanel
	--expandable embedded read-only preview of target Node


PropertyValue -> HTML renderers

Linker
    --fix linker to work with simple queries of craigslist data

Link Panel
    --enhance display ; add more information about the link
    --allow navigation to link target (display in new window?)

DetailEditPanel
	--improve 'Send' functionality
	--improve prompt wizard
        --global font size adjust
        --fix bottom bar's buttons - they disappear if the window is too small. need scrollpane?


Pattern w/ Default Values = Template

Create Template from Detail (add as subclass of one or more of its existing parents)

Add "RSS Channel" Pattern
	--with action to refresh the RSS items
	--Invokes 'Refresh RSS' on it
        --add action to remove associated items (same for Craigslist channel)

Ontology statistics view: # of classes, # of instances, etc..

Gnutella aggregation

"When" View
    --add filter (which can be re-used for other views)
        keyword
        list of all unique patterns and checkbox to indicate their visibility

"Where" View

OWL Import
    --Ignore inheriting from "Thing" to set classes as top level patterns

Global Threadpool for Long-running Asynchronous Tasks
    --in Self?

FindPatternPanel - Pattern and Property Search Dialog

Alphabetize Patterns Menu and Sub-menus

OpenCalais / Zemanta Support

Google Results

Cache "Pattern Menu" in SelfBrowserPanel to avoid recreating it each time new detail is displayed
    --may be useful in SelfBrowserPanel itself